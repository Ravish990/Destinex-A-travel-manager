import React from 'react';
import '../../App.css';

export default function Products() {
  return <h1 className='products'>PRODUCTS</h1>;
}
// The Products component is a simple functional component that renders a heading with the text "PRODUCTS".
// It is styled with a CSS class named 'products'.